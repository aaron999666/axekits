{
  "name": "toolbox-api",
  "version": "1.0.0",
  "private": true,
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute toolbox-db --file=schema.sql --remote",
    "db:local": "wrangler d1 execute toolbox-db --file=schema.sql --local"
  },
  "dependencies": {},
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250401.0",
    "typescript": "^5.7.0",
    "wrangler": "^4.0.0"
  }
}
